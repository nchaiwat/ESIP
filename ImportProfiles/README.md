# Legacy KPI import profiles

The YAML files in this directory describe the retired KPI-by-SKU workbook prototype.
They remain temporarily for regression-test compatibility only.

Active onboarding uses the Daily Raw evidence in `config/daily_raw_profiles.yaml` and
the source-specific Daily Raw loaders. Do not use these legacy profiles for a production
load or place KPI workbooks back into `SourceFiles`.
